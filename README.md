# Warehouse App

Краткое руководство по запуску и новым возможностям FIFO

Описание
- Streamlit-приложение для складского учёта: товары, накладные (приход), чеки (продажи), остатки, отчёты, интеграция с маркетплейсом.

Запуск (локально)
1. Установите зависимости (рекомендуется виртуальное окружение):

```bash
pip install streamlit pandas openpyxl xlsxwriter
```

2. Запустите приложение:

```bash
streamlit run warehouse_app.py
```

FIFO (учёт списания по партиям)
- Добавлена таблица `sale_fifo` и логика пересчёта FIFO для каждого чека.
- В интерфейсе:
  - В разделе `Просмотр базы` → `Продажи (чеки)` при редактировании чека появилась кнопка `🔁 Пересчитать FIFO для этого чека` — пересчитывает списание партий для текущего чека.
  - В разделе `📊 Отчёты` добавлена административная кнопка `🔁 Пересчитать FIFO для всех чеков (начиная с FIFO_START_DATE)` — выполняет массовый пересчёт для всех чеков начиная с даты `FIFO_START_DATE` (по умолчанию `2026-01-01`).

Логирование
- Все события пересчёта FIFO и ошибки логируются в файл `fifo_recalc.log` рядом с файлом `warehouse_app.py`.
- Если массовый пересчёт выдаёт ошибки, они будут показаны в интерфейсе и записаны в лог.

Примечания
- По умолчанию массовый пересчёт ограничен датой `FIFO_START_DATE`. Это сделано для безопасности и производительности. Если нужно пересчитать более ранние периоды (например 2025), сначала протестируйте на копии БД.
- Рекомендация: перед массовым пересчётом сделать резервную копию файла `warehouse.db`.
